name: Check

on:
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Unit Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run check
        run: make test
