name: Test
on:
  pull_request:
    branches: [main]
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v2
      - name: Test
        run: make test
